# 1. データセットを n 個のブロックに分割する.

# ----- ----- ----- ----- ----- # 1
# データセットを n 個のブロックに分割する.

# positive
tid = 0
data = {}
for line in open('positive.txt', 'r'):
    data[tid]=line
    tid += 1
print(tid)


# 2. ひとつのブロックを評価データ，のこりを学習データとする.
# 3. 評価データに採用するブロックを変化させながら，学習&評価を n 回繰り返す. 

# 4. さいごに，評価値の平均値を計算する.